{% extends 'ABSTRACT/entity/show.html.twig' %}

{% set title    = "Domaine : "~domain.name %}
{% set item     = domain %}

{% set showedValues = {
} %}

{% for category in domain.categories %}
    {% set label = 'Catégorie '~loop.index %}
    {% set showedValues = showedValues|merge({
        (label) :     domain.categories[loop.index0].name
    }) %}
{% endfor %}
